require 'spec_helper'

describe Pod::Check do
  it 'has a version number' do
    expect(Pod::Check::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
