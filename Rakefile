require "bundler/gem_tasks"

task :default => :test

#-- Tests ------------------------------------------------------------------#

desc "Runs all the rspec tests"
task :test do
  sh "bundle exec rspec"
end
